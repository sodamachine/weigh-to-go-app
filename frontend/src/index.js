// let dayForm = document.getElementById('dayForm')
// dayForm.addEventListener('submit', Day.postDay)

// Day.fetchDays()

Calendar.appendCalendar()

function jsonToJS(resp) {
    return resp.json()
}